@{
    Layout = null;   
}
    <div>
        <div>
            <table>
                <tr>
                    <td>Id</td>
                    <td>Name</td>
                    <td>Email</td>
                    <td>Age</td>
                </tr>
                <tr ng-repeat="y in names">
                    <td>{{y.Id}}</td>
                    <td>{{y.Name}}</td>
                    <td>{{y.Email}}</td>
                    <td>{{y.Age}}</td>
                </tr>
            </table>

        </div>
        </div>
